{
  "$schema": "https://opencode.ai/config.json",
  "references": {
    "example-ztp": {
      "path": "~/repos/lab/ansible_cat-onboarding",
      "description": "an example project using a ztp server and jinja templates to onboard a catalyst switch"
    }
  }
}